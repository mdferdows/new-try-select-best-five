
function amountCalculation(id) {
    const elementField = document.getElementById(id);
    const elementFieldString = elementField.value;
    const elementAmount = parseFloat(elementFieldString);
    elementField.value = '';

    elementAmount;

}